# MimicWX-Linux 🐧

零风险微信自动化框架 — 基于 AT-SPI2 无障碍接口 + uinput 内核级输入仿真

> Zero-risk WeChat automation framework for Linux via AT-SPI2 + uinput kernel input

## 特性

- 🔍 **AT-SPI2 消息检测** — 通过无障碍接口读取微信消息，无需注入进程
- ⌨️ **uinput 输入仿真** — 内核级虚拟设备，等同真实硬件输入，零 Synthetic 标记
- 🎭 **拟人化引擎** — 高斯分布按键延迟 + 贝塞尔曲线鼠标轨迹
- 🔌 **OneBot v11 API** — HTTP + WebSocket，可对接 Yunzai 等框架
- 🐳 **Docker 一键部署** — Xvfb 虚拟显示 + VNC 远程查看

## 架构

```
┌─ Docker 容器 ─────────────────────────────┐
│  Xvfb (虚拟显示) + VNC/noVNC              │
│  微信 Linux 版                             │
│                                           │
│  MimicWX (Rust)                           │
│  ├── AT-SPI2 → 读取消息 (只读，零风险)     │
│  ├── uinput  → 模拟输入 (内核级，不可检测) │
│  ├── 拟人化  → 随机延迟 (像真人操作)       │
│  └── API     → OneBot v11 对外服务        │
└───────────────────────────────────────────┘
```

## 快速开始

### 环境要求

- Linux 系统 (Ubuntu 22.04 推荐)
- Docker
- `/dev/uinput` 设备

### 部署

```bash
git clone https://github.com/PigeonCoders/MimicWX-Linux.git
cd MimicWX-Linux
docker build -t mimicwx .
docker run -d --name mimicwx \
  --device /dev/uinput \
  -p 5901:5901 \
  -p 6080:6080 \
  -p 8899:8899 \
  mimicwx
```

### 访问

| 服务 | 地址 | 说明 |
|------|------|------|
| noVNC | `http://HOST:6080/vnc.html` | 浏览器远程桌面（密码: `mimicwx`）|
| VNC | `vnc://HOST:5901` | VNC 客户端连接 |
| API | `http://HOST:8899` | OneBot v11 接口 |

## 技术栈

| 组件 | 技术 |
|------|------|
| 语言 | Rust |
| 异步运行时 | Tokio |
| 消息检测 | AT-SPI2 (atspi-rs + zbus) |
| 输入仿真 | uinput (evdev) |
| API 服务 | axum (HTTP + WebSocket) |
| 容器化 | Docker (Ubuntu 24.04) |

## License

MIT
